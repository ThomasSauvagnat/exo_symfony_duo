{% extends 'base-admin.html.twig' %}

{% block title %}{{forum.title}}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{forum.title}}</h1>
        <p>Date de création : {{forum.createdAt | date("d/m/Y")}}</p>

        <a href="{{ path('app_admin_forum') }}"><button class="btn btn-secondary mt-3">Retour</button></a>
    </div>

{% endblock %}
