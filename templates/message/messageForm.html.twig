{% extends 'base.front.html.twig' %}

{% block title %}Nouveau message{% endblock %}

{% block body %}
    <div class="container">
        <h1>Nouveau message</h1>
        <div class="row">
            {{form_start(form)}}
                {{form_row(form.content)}}
                <button type="submit" class="btn btn-secondary">Créer le message</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
