<nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top fs-3 fw-bold">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">Steamish</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse text-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active text-white" aria-current="page" href="{{ path('app_publisher') }}">Editeurs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('app_game') }}">Jeux</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white" href="{{ path('app_account_all') }}">Comptes</a>
        </li>
        <li class="nav-item">
          <a class="nav-link text-white btn btn-primary" href="{{ path('app_account_new') }}">Inscription</a>
        </li>
        {% if is_granted("ROLE_ADMIN") %}
          <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_admin') }}">DashBoard</a>
          </li>
        {% endif %}
      </ul>
    </div>

    {# // Permet d'afficher le rendu d'une reponse d'un controller => affiche la navbar
    Le nom du controller :: le nom de la fonction à utiliser
    On veut afficher le resultat d'une fonction d'un controleur et non une route
     #}
    {{ render(controller(
      'App\\Controller\\SearchController::index'
    )) }}
  </div>
</nav>
<div class="mt-5 pt-5"></div>